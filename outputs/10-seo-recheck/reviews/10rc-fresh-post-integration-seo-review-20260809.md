# 10R-C Fresh Post-Integration SEO Review — mistfall-hunter

- Project: `mistfall-hunter` / `mistfallhunter.co`
- Reviewer: 市场侦察-01 / SEO-GEO-AEO 研究 Agent (profile `seo`)
- Date: 2026-08-09
- Authority: `inputs/02-owner-confirmations/owner-release-10R-B-and-launch-prep-20260809.md`
- Mode: read-only, local only. No source edits, Git, deploy, DNS, Cloudflare, analytics, GSC/Bing/IndexNow, or public actions performed.
- Active topic: `telegram:-1004485313257:4537` (build phase per topic map)

## Verdict

**PASS** (integration contract verified independently against live local production build)

One P1 observation (not a blocking regression) is recorded in §7 — FAQ text reuse that originates in the A3 frozen copy package, not in the frontend integration.

## Method (independent, not trust-on-copy)

- Started local production Next server from `.next/standalone` at `127.0.0.1:3100` (the build generated by the 10R-B fresh `npm run build`; server log `/tmp/mistfall-preview-10rc.log`).
- Crawled all 58 sitemap URLs and validated status, TDK, canonical, robots meta, JSON-LD schema, FAQ counts, SSR rows and rendered word counts.
- Cross-checked every rendered field against the canonical A3 copy package (`outputs/05-copy-repair/routes/**/*.json`, 54 files) plus frozen FAQ contract (`outputs/05-copy/faq-schema-copy.md`).
- Re-ran the upstream 10R-B audit script (`audit_10r_b_static.py`) from `/tmp` (no workspace writes) and confirmed its JSON output reproduces exactly — upstream evidence is trustworthy.
- Audit scripts: `/tmp/10rc_seo_audit.py`, `/tmp/10rc_jaccard.py`; raw results `/tmp/10rc-audit-results.json`, `/tmp/10rc-jaccard-main.json`.

## 1. Route matrix: 54 copy routes / 58 sitemap paths — PASS

- `sitemap.xml` returns HTTP 200 with **58 unique URLs**, all crawled HTTP 200.
- `lib/copy.ts` (`copyByPath`) contains **54 copy routes**; all 54 are present in sitemap; the remaining 4 are trust routes (`/about /privacy /terms /contact`) which complete 58.
- No copy route missing from sitemap; no unexpected extra indexable route without copy.
- 404 route (`/definitely-not-a-route-xyz`) returns HTTP 404 with `robots: noindex`, and `/404` is NOT in sitemap.

## 2. Metadata / canonical / schema / FAQ / SSR contract — PASS

Independent DOM-level checks (all 58 routes):

| Check | Result |
|---|---|
| `<title>` exact match vs frozen copy | 54/54 |
| meta description exact match vs frozen copy | 54/54 |
| directAnswer rendered in body | 54/54 |
| all frozen editorial fragments rendered (H2/paragraphs/bullets/tables/FAQ q+a) | 54/54 |
| canonical exact self-reference (58 = 54 copy + 4 trust + home) | 58/58 |
| FAQPage JSON-LD present | 54/54 |
| FAQ count ≥ 3 per copy route | 54/54 (total 182 questions) |
| JSON-LD parse errors | 0 |
| Article/WebApplication/WebSite/Organization/BreadcrumbList/ItemList schema wiring | as specified (home: WebSite+FAQPage+Organization; tools: WebApplication+FAQPage+BreadcrumbList; content: Article+FAQPage+BreadcrumbList; /about: Organization; /items & /tier-list: ItemList) |
| BreadcrumbList | 53 copy/content routes (matches 10R-B) |
| OG + Twitter meta on representative routes (home/guide/tool/tier) | present |

SSR data contract:

- `/items` — server-rendered 48 item rows (Browse the 48-item seed database table)
- `/loot-finder` — server-rendered 48 item rows
- `/map` — server-rendered 24 POI names (≥ 4 required threshold)
- Data source `public/data/items.json` (48 items), `public/data/map-pois.json` verified locally.

## 3. robots / sitemap indexability policy — PASS

- `robots.txt` (served by `app/robots.ts`): `User-Agent: * / Allow: / / Disallow: /api/, /404` + `Sitemap: https://mistfallhunter.co/sitemap.xml`.
- No `noindex` robots meta on any of the 58 sitemap paths (all indexable).
- 404 page is `noindex, nofollow`; not in sitemap.
- No `X-Robots-Tag` surprises on sampled routes (`/`, `/guides/tips`, `/items`).

## 4. Thin / templated-content regression — PASS

- Word-spec (upstream definition incl. FAQ q+a, per `qa_copy_a3.py` body()): **0 failures** on all 54 copy routes. Content pages ~1,900–2,500 rendered words; tool pages within 500–800 band (rendered 442–977 with interactive panels); home exempt (1,031 rendered).
- Replayed upstream 5-shingle Jaccard over rendered `<article>` bodies: **0.1938** max pair `/builds/shadowstrix` ↔ `/builds/sorcerer` — far below the 0.40 gate.
- A stricter unigram Jaccard over `<main>` bodies shows higher raw numbers (max 0.54) — expected, because the 6 build/class pages share game vocabulary and section scaffolding (`Common Mistakes`, `Squad Context`, `Weapon and Gem Shopping Guide`, `Progression Roadmap`, FAQ/Related headings). Manual review of the highest-similarity pairs shows class-specific opening lines and distinct body prose (Sorcerer = burst/cooldown economy; Shadowstrix = mobility/stealth cycle; Blackarrow = ranged/kiting), so this is shared-vocabulary similarity, not door-page duplication. Verdict consistent with the skill rule: compare only `<main>/<article>` visible body; 5-shingle measure is the template detector and it passes.
- No frozen-copy `[DATA: ...]`/TBD placeholder was promoted as SEO body text (per `qa_copy_a3.py` template/generated/ban checks replayed in A3 receipt; no placeholder regression found in rendered DOM).

## 5. Independent re-run of upstream 10R-B audit — reproduced

- Copy of `outputs/10-seo-recheck/audit_10r_b_static.py` executed from `/tmp` against the same live server: all summary values reproduce exactly (`copy_routes 54`, `sitemap_routes_expected 58`, `word_spec_pass 54`, `faq_at_least_3_pass 54`, `faqpage_count 54`, `frozen_title/meta/direct_answer/editorial exact 54`, `canonical_exact_pass 58`, `organization_home/about true`, `breadcrumb_count 53`, `itemlist_paths /items,/tier-list`, `items/loot SSR 48`, `map_ssr_poi_names 4`, `max_rendered_jaccard 0.1938`, `pass true`).

## 6. What was NOT done (per authority/non-goals)

- No Git commit/push, no deploy, no Cloudflare/DNS/custom-domain, no cache purge, no GSC/Bing/IndexNow, no analytics, no public actions, no source edits.
- Production status of `mistfallhunter.co` remains unclaimed; this verdict covers the local reconciled A3 frontend only.

## 7. Observations (non-blocking, downstream decision)

- **P1 — FAQ text reuse across routes (source: A3 frozen copy package, not integration):**
  - `Should I use performance or quality mode?` — identical Q + nearly identical A on `/guides/ps5-settings` and `/guides/xbox-settings` (both 107 chars: "Performance mode favors frame rate for PvP; quality mode favors visuals. The guide recommends per scenario.").
  - `Is Mistfall Hunter free to play?` — same question on `/` and `/guides/faq`, different answers (acceptable pattern but flagged for dedupe consideration).
  - `Is Mistfall Hunter crossplay?` — same question on `/guides/faq` and `/guides/getting-started`, different answers.
  - These allocations come from the frozen FAQ contract (`outputs/05-copy/faq-schema-copy.md`) which the A3 scanner treats as allowed pairs; the integration renders them faithfully. Not a 10R-C regression. Recommended owner/文案 decision before production: rewrite the ps5/xbox duplicate answer so each settings page answers with platform-specific framing, or keep as-is if the short shared answer is accepted. SEO risk: mild duplicate-snippet risk on two sibling pages; does not block indexing.

## 8. Acceptance checklist

- [x] sitemap only indexable real pages (58, all 200, no 404/redirect in sitemap)
- [x] unique H1/title/meta/canonical per core page (title/meta exact vs frozen; canonical self-referencing)
- [x] schema present and parseable (Article/FAQPage/WebApplication/WebSite/Organization/BreadcrumbList/ItemList)
- [x] robots policy correct (allow `/`, disallow `/api/`, `/404`; sitemap referenced)
- [x] SSR contract (items 48 / loot-finder 48 / map POI 24)
- [x] no thin/templated content regression (word spec 0 fail; 5-shingle Jaccard 0.1938 < 0.40)
- [ ] GSC/Bing/IndexNow — NOT in scope (requires production + owner account actions)

## 9. Handoff to downstream

- Next: PM gate (02P re-review), compliance re-review, QA acceptance (mobile 360/390/430), then launch prep per owner release.
- SEO release recommendation: local integration is index-safe and PASS; production indexing actions (GSC verify, sitemap submit, IndexNow) remain blocked on real deployment + account permissions.
